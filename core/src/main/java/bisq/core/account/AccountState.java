package bisq.core.account;

// Todo: should be persisted on disk
public class AccountState implements java.io.Serializable {

    // Temp in memory variable for testing.
    public boolean accountExists = false;

    // Temp in memory variable for testing.
    public String password = null;
}
